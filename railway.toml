[build]
builder = "nixpacks"
watchPatterns = ["backend/**"]
buildCommand = "cd backend && pip install -r requirements.txt"

[deploy]
startCommand = "cd backend && gunicorn wsgi:app --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 120"
restartPolicyType = "ON_FAILURE"
